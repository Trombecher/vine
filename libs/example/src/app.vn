use esnext.document;
use aena.*;

fn main() {
    mount(document.body, <App/>);
}

fn App() {
    <div>
        <h1>Hello, World!</h1>
        {["test", "test2"].map(fn(s) <a href={"/{s}"}>{s.pascal_case()}</a>)}
    </div>
}