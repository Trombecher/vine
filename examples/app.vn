/// A function to compute the GCD of two numbers.
pub fn gcd(a: num, b: num) -> num {
    if b == 0 {
        a
    } else {
        gcd(b, a % b)
    }
}

fn main() {
    <gcd a={10} b={20}/>
}